<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      /* General Styles */
      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        background-color: #f9fafb;
        margin: 0;
        padding: 0;
        color: #111827;
        line-height: 1.5;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
      }

      /* Header */
      .header {
        background: linear-gradient(135deg, #0ea5e9 0%, #0369a1 100%);
        color: white;
        text-align: center;
        padding: 30px 20px;
      }

      .header svg {
        width: 60px;
        height: 60px;
        margin-bottom: 15px;
      }

      .header h1 {
        font-size: 26px;
        margin: 0;
        font-weight: 700;
        letter-spacing: 0.5px;
      }

      /* Content */
      .content {
        background-color: white;
        padding: 35px;
      }

      .content h2 {
        font-size: 22px;
        margin-top: 0;
        color: #0369a1;
      }

      .content p {
        font-size: 16px;
        margin-bottom: 20px;
        color: #374151;
      }

      .ticket-info {
        background-color: #f3f4f6;
        border-radius: 6px;
        padding: 15px;
        margin-bottom: 30px;
      }

      .ticket-id {
        color: #0284c7;
        font-weight: 600;
        font-size: 16px;
        margin-bottom: 4px;
      }

      .ticket-title {
        font-weight: 600;
        margin-bottom: 0;
      }

      /* Comment Box */
      .comment-box {
        border: 1px solid #e5e7eb;
        border-radius: 8px;
        padding: 20px;
        margin: 30px 0;
      }

      .comment-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
      }

      .comment-author {
        font-weight: 600;
        color: #111827;
      }

      .comment-time {
        color: #6b7280;
        font-size: 14px;
      }

      .comment-body {
        background-color: #f9fafb;
        border-radius: 6px;
        padding: 15px;
        color: #1f2937;
      }

      .comment-body p {
        margin: 0;
        white-space: pre-wrap;
      }

      .btn-container {
        text-align: center;
        margin: 30px 0 10px;
      }

      .btn {
        display: inline-block;
        background-color: #0284c7;
        color: white;
        text-decoration: none;
        padding: 14px 24px;
        border-radius: 6px;
        font-weight: 600;
        transition: all 0.2s ease;
        font-size: 16px;
      }

      .btn:hover {
        background-color: #0369a1;
      }

      .reply-prompt {
        font-size: 15px;
        text-align: center;
        color: #4b5563;
        margin: 20px 0 0;
      }

      /* Footer */
      .footer {
        background-color: #f8fafc;
        padding: 25px;
        text-align: center;
        color: #64748b;
        font-size: 14px;
        border-top: 1px solid #e2e8f0;
      }

      .logo {
        font-weight: 700;
        color: #0284c7;
        margin-bottom: 10px;
        display: block;
        font-size: 16px;
      }
    </style>
    <title>New Comment on Your Ticket - SupportSphere</title>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path
            d="M18 2L22 6V18C22 19.1046 21.1046 20 20 20H4C2.89543 20 2 19.1046 2 18V6C2 4.89543 2.89543 4 4 4H10L12 2H18Z"
            fill="white"
          />
          <path
            d="M9 13H15M12 10V16"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
        <h1>SupportSphere</h1>
      </div>
      <div class="content">
        <h2>New Comment on Your Ticket</h2>

        <p>Hi <%= user.firstName %>,</p>

        <p><%= comment.author %> has added a comment to your support ticket:</p>

        <div class="ticket-info">
          <div class="ticket-id">#<%= ticket.id %></div>
          <p class="ticket-title"><%= ticket.title %></p>
        </div>

        <div class="comment-box">
          <div class="comment-header">
            <div class="comment-author"><%= comment.author %></div>
            <div class="comment-time"><%= comment.createdAt %></div>
          </div>
          <div class="comment-body">
            <p><%= comment.text %></p>
          </div>
        </div>

        <div class="btn-container">
          <a href="<%= ticketUrl %>" class="btn">View Ticket</a>
        </div>

        <p class="reply-prompt">
          You can reply to this comment by logging into SupportSphere and adding
          a new comment to the ticket.
        </p>
      </div>
      <div class="footer">
        <span class="logo">SupportSphere</span>
        <p>
          Please do not reply to this email. If you need to provide additional
          information, please add a comment to your ticket.
        </p>
        <p>
          © <%= new Date().getFullYear() %> SupportSphere. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>
