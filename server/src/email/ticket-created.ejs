<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      /* General Styles */
      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        background-color: #f9fafb;
        margin: 0;
        padding: 0;
        color: #111827;
        line-height: 1.5;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
      }

      /* Header */
      .header {
        background: linear-gradient(135deg, #0ea5e9 0%, #0369a1 100%);
        color: white;
        text-align: center;
        padding: 30px 20px;
      }

      .header svg {
        width: 60px;
        height: 60px;
        margin-bottom: 15px;
      }

      .header h1 {
        font-size: 26px;
        margin: 0;
        font-weight: 700;
        letter-spacing: 0.5px;
      }

      /* Content */
      .content {
        background-color: white;
        padding: 35px;
      }

      .content h2 {
        font-size: 22px;
        margin-top: 0;
        color: #0369a1;
      }

      .content p {
        font-size: 16px;
        margin-bottom: 20px;
        color: #374151;
      }

      /* Ticket Card */
      .ticket-card {
        border: 1px solid #e5e7eb;
        border-radius: 8px;
        padding: 20px;
        margin: 30px 0;
        background-color: #f9fafb;
      }

      .ticket-header {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
        align-items: center;
      }

      .ticket-id {
        font-weight: 600;
        font-size: 20px;
        color: #0284c7;
      }

      .ticket-status {
        background-color: #bfdbfe;
        color: #1e40af;
        padding: 5px 12px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 500;
      }

      .ticket-title {
        font-size: 18px;
        font-weight: 600;
        margin: 12px 0;
        color: #111827;
      }

      .ticket-meta {
        color: #6b7280;
        font-size: 14px;
        margin-bottom: 15px;
      }

      .ticket-meta span {
        display: inline-block;
        margin-right: 15px;
      }

      .priority-high {
        color: #dc2626;
      }

      .priority-medium {
        color: #d97706;
      }

      .priority-low {
        color: #2563eb;
      }

      .ticket-description {
        padding: 15px 0;
        border-top: 1px solid #e5e7eb;
      }

      .ticket-description p {
        margin: 0;
      }

      .btn-container {
        text-align: center;
        margin: 30px 0 10px;
      }

      .btn {
        display: inline-block;
        background-color: #0284c7;
        color: white;
        text-decoration: none;
        padding: 14px 24px;
        border-radius: 6px;
        font-weight: 600;
        transition: all 0.2s ease;
        font-size: 16px;
      }

      .btn:hover {
        background-color: #0369a1;
      }

      /* Footer */
      .footer {
        background-color: #f8fafc;
        padding: 25px;
        text-align: center;
        color: #64748b;
        font-size: 14px;
        border-top: 1px solid #e2e8f0;
      }

      .logo {
        font-weight: 700;
        color: #0284c7;
        margin-bottom: 10px;
        display: block;
        font-size: 16px;
      }

      .help-text {
        margin: 20px 0 0;
        font-size: 14px;
        color: #64748b;
      }
    </style>
    <title>Your Support Ticket Has Been Created - SupportSphere</title>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path
            d="M18 2L22 6V18C22 19.1046 21.1046 20 20 20H4C2.89543 20 2 19.1046 2 18V6C2 4.89543 2.89543 4 4 4H10L12 2H18Z"
            fill="white"
          />
          <path
            d="M9 13H15M12 10V16"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
        <h1>SupportSphere</h1>
      </div>
      <div class="content">
        <h2>Your Ticket Has Been Created</h2>

        <p>Hi <%= user.firstName %>,</p>

        <p>
          Your support request has been received and a ticket has been created.
          Our support team will look into this as soon as possible.
        </p>

        <div class="ticket-card">
          <div class="ticket-header">
            <div class="ticket-id">#<%= ticket.id %></div>
            <div class="ticket-status"><%= ticket.status %></div>
          </div>

          <div class="ticket-title"><%= ticket.title %></div>

          <div class="ticket-meta">
            <span class="priority-<%= ticket.priority.toLowerCase() %>">
              Priority: <%= ticket.priority %>
            </span>
            <span>Category: <%= ticket.category %></span>
            <span>Created: <%= ticket.createdAt %></span>
          </div>

          <div class="ticket-description">
            <p><%= ticket.description %></p>
          </div>
        </div>

        <div class="btn-container">
          <a href="<%= ticketUrl %>" class="btn">View Ticket</a>
        </div>

        <p class="help-text">
          You will receive email notifications when there are updates to your
          ticket. You can also check the status of your request at any time by
          visiting our support portal.
        </p>
      </div>
      <div class="footer">
        <span class="logo">SupportSphere</span>
        <p>We aim to respond to all tickets within 24 hours.</p>
        <p>
          © <%= new Date().getFullYear() %> SupportSphere. All rights reserved.
        </p>
      </div>
    </div>
  </body>
</html>
